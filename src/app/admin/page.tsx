 export default function AdminPage() {
   return (
     <main className="container">
       <section>
         <div className="card">
           <div style={{ fontWeight: 600, marginBottom: 8 }}>System Admin Dashboard</div>
           <div style={{ color: 'var(--muted)' }}>Welcome, Admin.</div>
         </div>
       </section>
     </main>
   );
 }
